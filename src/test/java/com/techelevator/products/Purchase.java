package com.techelevator.products;

import junit.framework.TestCase;

public class Purchase extends TestCase {
    /*
       test current money provided

       test feed money
                brings current money provided up by one whole dollar
       test select product
                returns matching kvp between product and map?
                if sold out, display message
                if not sold out, dispense
                if not sold out dispense and display messages (maybe sound)
                if not sold out, calculate change (maybe in finish)

       test finish transaction
                gives back any change
                    returns money using nickles, dimes, quarters (using small amount possible)
                    math?
                    current bal updates to zero
                    returns to main menu



     */

}
